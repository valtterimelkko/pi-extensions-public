# Todo Extension

A small task-tracking extension for the Pi Coding Agent.

## What it does

- registers a `todo` tool for the model
- registers a `/todos` UI command for the user
- keeps todo state session-aware through Pi session history

## Use cases

Useful when you want the agent to maintain a lightweight checklist during a coding or planning session without relying on external files.
